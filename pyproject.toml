[project]
name = "vim"
version = "0.1.0"
dependencies = ["pynvim>=0.5.2", "pip>=25.1.1"]
requires-python = ">=3.12,<3.13"

[tool.uv]
managed = true

[tool.uv.workspace]
members = []

[tool.setuptools]
py-modules = []
